# v1.0.2 Release Notes

v1.0.2 is an unsigned documentation-sync patch release.

Release:

```text
https://github.com/Staros-Labs/course-package-tester/releases/tag/v1.0.2
```

Artifact:

```text
CoursePackageTester_Windows_x64_v1.0.2.zip
```

## Signature Status

v1.0.2 is unsigned. Windows may show a publisher or SmartScreen warning.

Windows code signing remains documented and ready, but is deferred until a certificate or signing provider is available.

## Shipped Changes

- Syncs the packaged Windows documentation with the current public repository docs.
- Updates the agency handoff template for v1.0.2.
- Adds first-run agency feedback prompts.
- Keeps the v1.0.1 dependency, manifest parser, handoff, and signed-release operations updates.
- Keeps the app local-only with no telemetry, upload, cloud, or auto-update behavior.

## Validation

- `npm ci`
- `npm test`
- `npm run package:windows:zip`
- GitHub Actions CI passed on `main`.
- GitHub Actions CI passed on `v1.0.2`.
- CodeQL passed on `main`.

## Remaining Follow-Ups

- Capture agency install, extraction, launch, and folder-selection feedback.
- Improve messaging for zips with multiple possible launch files.
- Improve diagnostics for packages with missing or malformed manifests.
- Review Tester Report wording after first external use.
- Add signed Windows releases after certificate cost is approved.
